version: '3.8'
services:
  podcast-assistant:
    image: ghcr.io/pelagiagk/podcast-assistant:latest
    container_name: pyannote-app
    restart: unless-stopped
    ports:
      - "7860:7860"
    env_file:
      - .env